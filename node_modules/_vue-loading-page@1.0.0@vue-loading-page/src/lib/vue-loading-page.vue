<!-- 
    type:加载图标类型
        1.circle：实线转圈  
        2.dot：虚线转圈
        3.text：加载中... default
        4.
 -->
<template>
   <div>
       <div class="circle" v-if="type=='circle'"></div>
       <div class="dot" v-if="type=='dot'"></div>
       <div class="text" v-if="type=='text'">加载中<a class="dots"></a></div>
   </div>
</template>
<script>
export default {
   name: 'vue-loading-page',
   props: {
       type: {
           type: String,
           default: () => {
               return 'text'
           }
       },
       color: {
           type: String,
           default: () => {
               return 'black'
           }
       },
       speed: {
           type: String,
           default: () => {
               return '1000';
           }
       }
   }
}
</script>
<style scoped>
    .circle{
        width: 16px;
        height: 16px;
        margin: 0 auto;
        border: 2px solid currentColor;
        border-radius: 50%;
        border-top: 2px solid #F3F3F3;
        -webkit-animation: circle 2s linear infinite;
        animation: circle 2s linear infinite;
    }

    @-webkit-keyframes circle {
        0%{
            -webkit-transform: rotate(0deg);
        }
        100%{
            -webkit-transform: rotate(360deg);
        }
    }
    @keyframes circle {
        0%{
            transform: rotate(0deg);
        }
        100%{
            transform: rotate(360deg);
        }
    }

    .dot{
        width: 16px;
        height: 16px;
        margin: 0 auto;
        border: 2px dotted currentColor;
        border-radius: 50%;
        -webkit-animation: dot 2s linear infinite;
        animation: dot 2s linear infinite;
    }

    @-webkit-keyframes dot{
        0%{
            -webkit-transform: rotate(0deg);
        }
        100%{
            -webkit-transform: rotate(360deg);
        }
    }
    @keyframes dot {
        0%{
            transform: rotate(0deg);
        }
        100%{
            transform: rotate(360deg);
        }
    }

    .text{
        font-size: 12px;
        text-align: center;
        height: 20px;
        line-height: 20px;
    }
    .dots {
        display: inline-block;
        height: 1em;
        line-height: 1;
        text-align: left;
        vertical-align: -.25em;
        overflow: hidden;
    }
    .dots::before {
        display: block;
        content: '...\A..\A.';
        white-space: pre-wrap;
        animation: dots 2s infinite step-start both;
    }

    @-webkit-keyframes dots {
        33% {
            -webkit-transform: translateY(-2em);
        }

        66% {
            -webkit-transform: translateY(-1em);
        }
    }
    @keyframes dots {
        33% {
            transform: translateY(-2em);
        }

        66% {
            transform: translateY(-1em);
        }
    }
</style>